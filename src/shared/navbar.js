import NavigationBar from 'react-native-navbar'
<NavigationBar
  title={{ title: "User", tintColor: 'black', }}
  leftButton={{ title: 'Back', }}
  rightButton={{ title: 'Forward', }}
  style={{ backgroundColor: "white", }}
  statusBar={{ tintColor: "white", }}
/>